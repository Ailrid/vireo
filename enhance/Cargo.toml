[package]
name = "audio"
version = "0.1.0"
edition = "2024"

[dependencies]
fundsp = {version = "0.23.0"}
wasm-bindgen = "0.2.117"
web-sys = { version = "0.3.95", features = ["console"] }

[lib]
crate-type = ["cdylib"]

[profile.release]
lto = true
opt-level = 3
panic = "abort"
